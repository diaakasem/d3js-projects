// Generated by CoffeeScript 1.6.3
(function() {
  define(["d3", "lodash"], function(d3, _) {
    var animate, config, load, parent, render;
    parent = d3.select("#chart");
    config = {
      width: parent.style('width').slice(0, -2),
      height: parent.style('height').slice(0, -2)
    };
    load = function(path, cb) {
      var days;
      days = {
        name: "Days",
        children: []
      };
      return d3.csv(path, function(e, data) {
        var map;
        map = function(data, field) {
          var grouped;
          data = _.filter(data, field);
          grouped = _.groupBy(data, field);
          return _.map(grouped, function(arr, name) {
            return {
              name: name,
              children: arr.length > 0 ? _.map(arr, function(e) {
                return _.omit(e, field);
              }) : []
            };
          });
        };
        days.children = map(data, 'DATE');
        _.each(days.children, function(date) {
          date.children = date ? map(date.children, 'TIME') : [];
          return _.each(date.children, function(time) {
            time.children = time ? map(time.children, 'EVENTTYPE') : [];
            return _.each(time.children, function(eventtype) {
              eventtype.children = eventtype ? map(eventtype.children, 'EVENT') : [];
              return _.each(eventtype.children, function(event) {
                event.children = event ? map(event.children, 'ADDITIONALDETAILS') : [];
                return _.each(event.children, function(details) {
                  return details.children = [];
                });
              });
            });
          });
        });
        return cb(days);
      });
    };
    animate = function(data) {
      var click, diagonal, duration, exec, h, i, root, toggle, toggleAll, tree, update, vis, w;
      w = config.width;
      h = config.height;
      i = 0;
      duration = 500;
      root = void 0;
      update = function(source) {
        var link, linkEnterExit, node, nodeEnter, nodes;
        nodes = tree.nodes(root).reverse();
        node = vis.selectAll("g.node").data(nodes, function(d) {
          return d.id || (d.id = ++i);
        });
        nodeEnter = node.enter().append("svg:g").attr("class", "node").attr("transform", function(d) {
          return "translate(" + source.y0 + ", " + source.x0 + ")";
        });
        nodeEnter.append("svg:circle").attr("r", 4.5).style("fill", function(d) {
          if (d._children) {
            return "lightsteelblue";
          } else {
            return "#fff";
          }
        }).on("click", click);
        nodeEnter.append("svg:text").attr("x", 8).attr("y", 3).text(function(d) {
          return d.name;
        });
        nodeEnter.transition().duration(duration).attr("transform", function(d) {
          return "translate(" + d.y + ", " + d.x + ")";
        }).style("opacity", 1).select("circle").style("fill", "lightsteelblue");
        node.transition().duration(duration).attr("transform", function(d) {
          return "translate(" + d.y + ", " + d.x + ")";
        }).style("opacity", 1);
        node.exit().transition().duration(duration).attr("transform", function(d) {
          return "translate(" + source.y + ", " + source.x + ")";
        }).style("opacity", 1e-6).remove();
        link = vis.selectAll("path.link").data(tree.links(nodes), function(d) {
          return d.target.id;
        });
        linkEnterExit = function(d) {
          var o;
          o = {
            x: source.x0,
            y: source.y0
          };
          return diagonal({
            source: o,
            target: o
          });
        };
        link.enter().insert("svg:path", "g").attr("class", "link").attr("d", linkEnterExit).transition().duration(duration).attr("d", diagonal);
        link.transition().duration(duration).attr("d", diagonal);
        link.exit().transition().duration(duration).attr("d", linkEnterExit).remove();
        return nodes.forEach(function(d) {
          d.x0 = d.x;
          return d.y0 = d.y;
        });
      };
      toggleAll = function(d) {
        if (d.children) {
          d.children.forEach(toggleAll);
          return toggle(d);
        }
      };
      toggle = function(d) {
        if (d.children) {
          d._children = d.children;
          return d.children = null;
        } else {
          d.children = d._children;
          return d._children = null;
        }
      };
      click = function(d) {
        toggle(d);
        return update(d);
      };
      tree = d3.layout.tree().size([h, w - 160]);
      diagonal = d3.svg.diagonal().projection(function(d) {
        return [d.y, d.x];
      });
      vis = d3.select("#chart").append("svg:svg").attr("width", w).attr("height", h).append("svg:g").attr("transform", "translate(40,0)");
      exec = function(json) {
        root = json;
        json.x0 = 800;
        json.y0 = 0;
        root.children.forEach(toggleAll);
        return update(root);
      };
      d3.select(self.frameElement).style("height", "2000px");
      return exec(data);
    };
    render = function(data) {
      var cluster, diagonal, exec, height, svg, width;
      width = config.width;
      height = config.height;
      cluster = d3.layout.cluster().size([height, width - 160]);
      diagonal = d3.svg.diagonal().projection(function(d) {
        return [d.y, d.x];
      });
      svg = d3.select("body").append("svg").attr("width", width).attr("height", height).append("g").attr("transform", "translate(40,0)");
      exec = function(root) {
        var link, links, node, nodes;
        nodes = cluster.nodes(root);
        links = cluster.links(nodes);
        link = svg.selectAll(".link").data(links).enter().append("path").attr("class", "link").attr("d", diagonal);
        node = svg.selectAll(".node").data(nodes).enter().append("g").attr("class", "node").attr("transform", function(d) {
          return "translate(" + d.y + ", " + d.x + ")";
        });
        node.append("circle").attr("r", 4.5);
        return node.append("text").attr("dx", 8).attr("dy", 3).style("text-anchor", function(d) {
          if (d.children) {
            return "end";
          } else {
            return "start";
          }
        }).text(function(d) {
          return d.name;
        });
      };
      d3.select(self.frameElement).style("height", "" + height + "px");
      return exec(data);
    };
    return {
      load: load,
      render: render,
      animate: animate,
      config: config
    };
  });

}).call(this);

/*
//@ sourceMappingURL=app.map
*/
