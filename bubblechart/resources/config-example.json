{
  // Width of the SVG element, in pixels
  width: 800,
  // Height of the SVG element, in pixels
  height: 400,
  // Height of the put and call option volume curves, in pixels
  volumeHeight: 50,
  // Selector for target element in which the SVG will be placed
  selector: "#chart"
}
